/* 단일 설정 파일 — 모든 산출물은 이 값으로 생성됩니다.
   스크립트가 CAL을 각자 덮어쓰지 않도록 여기 한 곳에서만 정의합니다. */
export const CANON = {
  /* 능력치 가정 */
  evMode:  'all252',  // 'all252' | 'offense' | 'bulk'  (508 예산 민감도 확인용)

  /* 기술 선택 */
  mix:      0,   // 0 = 최대 화력 가정(그 상대에게 가장 센 공격기를 항상 보유), 1 = 채용률 가중
  slots:    0,   // >0 이면 실채용 상위 N칸 안의 기술만 사용 (4 = 실제 4칸 제약)
  slotsAll: 1,   // slots 적용 시 변화기까지 포함해 상위 N칸을 센다
  noSuicide:1,   // 자폭류를 '이겼다'로 세지 않는다

  /* 턴 → 값 변환 */
  K:        3.0, // 턴 차이 1 = 값 1/3
  spd:      1.0, // 선공 보정
  cap:      99,  // 턴수 상한
  beta:     0.3, // 내구항 비중
  dgate:    2,   // 내구는 위협도에 비례해서만 값이 된다

  /* 회복 */
  setVar:   1,   // 회복기를 든 세트/안 든 세트를 채용률로 나눠 값을 섞는다(세트 일관성)
  sleepGate:0.5, // 세트 변형 안에서 잠자기를 들었으면 상대에게 쌓기 창을 내준다
  recHP:    1.4, // 회복기 보유 시 실효 HP 상한 배율
  recOFF:   0.7, // 회복 턴만큼의 화력 손실

  /* 쌓기(랭크업) */
  setup:    1,
  suGate:   3,   // 내가 이 턴수 안에 못 넘기면 상대가 쌓는다
  suMax:    1,   // 쌓기에 쓰는 턴 수

  /* 특성 */
  abIm:     1,   // 타입 무효 특성
  abHalf:   1,   // 타입 반감 특성
  abCond:   1,   // 조건부 경감(필터류=효과굉장만, 멀티스케일=만피 1발만)
  abIntim:  1,   // 위협
  abStab:   1,   // 적응력·변환자재
  mgAb:     1,
  bodyPress:1,   // 바디프레스는 공격 대신 자기 방어 스탯으로 계산
  multiHit: 1,   // 연속기 타수 반영 (power 표는 1타 위력) — 트리플악셀 20→120(20+40+60), 2~5회기 ×3.1   // 메가는 기본형 특성이 아니라 자기 특성을 쓴다(스카이스킨·페어리스킨 포함)

  /* 반동기 — 구현은 되어 있으나 기본 0.
     1:1 끝까지 싸우는 틀에서는 실제보다 과하게 벌점이 붙고(교체·회복이 없다),
     적합도도 ρ50 0.502 → 0.480으로 나빠져 껐다. recoilK=1로 켜서 직접 확인할 수 있다. */
  recoilK:  0,

  /* 상대 메가 — 상대도 한 경기 1회만 쓸 수 있다.
     종별 실효 확률 = 스톤 채택률 ÷ 그 팀의 스톤 보유자 수(래더 표본 평균). effmega.json */
  oppMega:  1,

  dittoPen: 1    // 메타몽 복사 지연
};
