# Champions 싱글 배틀 분석기 (v6)

포켓몬 챔피언스 **싱글 배틀**용. op.gg에서 데이터를 긁어 와서 다음 네 가지를 합니다.

1. **최적 파티**: 6마리와 각자의 도구·성격·스탯포인트·기술 4개
2. **기술 배치**: 내 파티를 넣으면 기술(과 도구·성격)을 추천. 아이템 중복 금지, 메가 1마리 규칙 적용
3. **선출·선봉**: 상대 6마리를 보면 내 6마리 중 누구 3마리를 낼지, 선봉은 누구인지
4. **교체**: 배틀 중 상대 필드 포켓몬(남은 HP)을 상대로 내 남은 포켓몬 중 누구를 낼지

v5(Node 파이프라인, 09-17 수집본)는 `legacy_v5/`에 그대로 보관했습니다. 이유는 아래 "v5에서 바뀐 점"에 있습니다.

## 설치

Python 3.10 이상과 numpy만 있으면 됩니다(스크래퍼는 표준 라이브러리만 씀).

```bash
python -m pip install numpy
```

## 사용법

```bash
python -m champions scrape                 # op.gg 수집 → data/raw/<오늘 날짜>/  (싱글 순위 전 종, 약 15분, 이미 받은 건 건너뜀)
python -m champions prep                   # 전 종 세트 최적화 + 상성 행렬 → data/processed/  (첫 실행 20~30분, --pool N 으로 상위 N종만)
python -m champions team                   # 1. 최적 파티
python -m champions moves --party my.txt   # 2. 내 파티 기술 배치
python -m champions pick --party my.txt --opp 보만다,한카리아스,누리레느,하마돈,갑주무사,루카리오   # 3. 선출
python -m champions switch --party my.txt --hp 100,60,0,100,35,100 --active 마스카나 --opp 메가리자몽Y --opp-hp 70
python -m champions ui --party my.txt      # 실전 선출 보드(HTML) — 상대 6마리를 탭하면 ①선봉 ②③
python -m champions check                  # 모델 점검
```

결과는 화면에 나오고 `out/`에 마크다운으로도 남습니다. `team`은 `out/recommended_party.txt`를 만들고, 이 파일을 그대로 `--party`에 넣을 수 있습니다.

### 실전 선출 보드 (`ui`)

`out/pick_board.html` 하나로 동작하고, 인터넷이 없어도 됩니다(글꼴만 온라인일 때 적용).
- 생성할 때 내 파티 6마리와 상대 후보 **싱글 순위 전 종**(메가형 포함)의 상성을 미리 계산해 넣습니다. 동족전도 세트가 다르므로 실제 값으로 계산합니다.
- 화면에서는 상대 6마리를 목록에서 탭하거나 검색해 고릅니다. 메가 가능한 칸을 탭하면 `자동(확률) → 메가 확정 → 메가 아님` 순서로 바뀝니다.
- 결과는 3/6 선출 게임을 브라우저에서 LP로 풀어 보여줍니다.
  - **① 선봉 ② ③** 카드
  - 선출 비율, 상대 예상 선출
  - **상대 선봉별 대응**(선봉 유지인지, 누구로 교체할지)
  - 6×6 상성표
- 계산은 `pick`과 같습니다(검증: 같은 상대에서 게임 값·선출·선봉 일치).
- 파티를 바꾸면 다시 생성하세요. `--party`를 생략하면 `out/recommended_party.txt`를 씁니다.

### 파티 파일 형식

한 줄에 한 마리입니다. 이름만 적어도 되고, 아는 정보만 더 적으면 됩니다. `|`로 나눈 칸의 순서는 상관없습니다.

```
한카리아스
메가보만다                      # 메가 이름으로 쓰면 스톤을 든 것으로 봄
누리레느 @ 자뭉열매
마스카나 @ 기합의띠 | 고집 | 2/32/0/0/0/32 | 트릭플라워, 트리플악셀, 기습, 깨트리기
```

스탯포인트는 H/A/B/C/D/S 순서입니다(0~32, 합 66). `--names 한카리아스,누리레느,...`처럼 쉼표로 이어 써도 됩니다.

주요 옵션:

- `team --must 한카리아스 --exclude 메타몽 --pool 80`: 꼭 넣을 포켓몬과 뺄 포켓몬, 후보 풀 크기
- `moves --free`: 채용 70% 이상 기술도 바꿀 수 있게 함. `--keep`: 입력한 기술은 고정하고 빈 칸만 채움
- `pick --opp-mega 메타그로스`: 메가인 걸 아는 상대. 상대를 `메가리자몽Y`처럼 메가 이름으로 적어도 됨

## 데이터 (op.gg, 브라우저 없이 HTTP로)

op.gg 페이지는 Next.js RSC라서 HTML 안의 `self.__next_f.push(...)` 청크에 JSON이 들어 있습니다. `champions/scrape.py`가 청크를 이어 붙인 뒤 필요한 객체만 잘라 냅니다. robots.txt는 `User-Agent: * Allow: /`이고, 요청 사이에 1초씩 쉽니다.

| 원천 | 내용 | 파일 |
|---|---|---|
| `/tier` | 시즌 싱글 순위 262종 | `tier.json` |
| `/replica-teams?page=N` | 사용자가 제출한 팀 479개(아이템·특성·성격·스탯포인트·기술까지 완전한 빌드). 1쪽에 전체 도감 352종, 기술 920종(타수·흡수·반동·랭크변화 메타 포함), 아이템 201종 사전이 같이 들어 있음 | `replica_teams.json`, `dex.json` |
| `/pokedex/<key>` (싱글 순위 **전 종**, 시즌 M-6 기준 262종) | 포켓몬별 싱글 통계: 기술·도구·특성·성격 채용률, 스탯포인트 분포, 파트너, **가장 많이 이긴/진 상대 30**, 이길 때/질 때 기술 | `pokemon/<key>.json` |

## 모델

**1:1 전투 시뮬레이터** (`engine.py`): 레벨 50, 개체값 31. 능력치는 `HP = 종족값+75+SP`, 나머지는 `⌊(종족값+20+SP)×성격⌋`입니다. 데미지는 본가 식에 평균 난수 0.925를 곱해 씁니다. 턴마다 우선도와 스피드로 순서를 정하고 다음을 반영합니다.

- 랭크 변화: 쌓기기, 용성군류 자기 하락, 상대 하락기, 위협과 그 반응 특성
- 반동·흡수(기술 메타의 drain 값), 생명의구슬, 울퉁불퉁멧, 까칠한피부
- 기합의띠, 옹골참, 탈, 멀티스케일, 자뭉열매, 반감 열매, 풍선, 스카프 고정
- 날씨·필드 특성(메가진화로 켜지는 날씨가 나중에 발동), 스킨류, 적응력, 변환자재, 테크니션, 확정 급소기, 연속기 타수
- 화상·독·마비·잠듦·씨뿌리기, 속박기(엉겨붙기·회오리불꽃 등, 5턴 동안 1/8, 조임밴드 1/6), 회복기, 반동 턴, 모으기 기술, 기습·속이기 조건, 날려버리기·흑안개
- **PP**: 기술마다 PP를 세고, 다 떨어지면 발버둥을 씁니다. 회복전은 임의 횟수가 아니라 PP로 끝납니다.
- 속이기: 첫 턴에만 쓸 수 있고 상대를 풀죽게 합니다. 정신력·인분과 사이코필드에는 막힙니다.
- 변환자재·리베로: 등장 후 첫 기술 때 **막는 타입까지** 그 기술 타입으로 바뀝니다(1회). 이후 자속은 바뀐 타입 기준입니다.
- 깨어진갑옷: 물리기를 맞으면 방어 −1, 스피드 +2가 됩니다.
- 연속기는 기합의띠와 옹골참을 뚫습니다. 탈은 첫 타만 막습니다. **멀티스케일·섀도실드·반감 열매도 첫 타만** 줄입니다(트리플악셀은 첫 타 비중 20/120). **울퉁불퉁멧·까칠한피부·철가시·정전기류는 맞은 타마다** 발동하고, 도중에 쓰러지면 쓰러뜨린 타까지만 셉니다.
- **방어류**(방어·판별·킹실드·니들가드 등): 그 턴의 공격·상태이상을 막고, 연속으로는 쓰지 않습니다. 킹실드는 킬가르도를 실드폼으로 돌리고, 니들가드는 접촉한 상대에게 1/8 피해를 줍니다.
- **희망사항**: 다음 턴 끝에 최대 HP의 1/2을 회복합니다. 희망사항 다음 턴에 방어가 있으면 방어로 버팁니다.
- **전기로바꾸기**: 맞으면 충전되어 다음 전기 기술 위력이 2배가 됩니다.
- **공격기의 부가효과**(C-3): 화상·마비·독·결빙·잠듦, 풀죽음, 확률 랭크 변화(문포스 특공 −1, 섀도볼 특방 −1, 코멧펀치 공격 +1 등)
  - 100% 효과(볼부비부비·전자포·연옥 등)는 바로 적용합니다.
  - 확률 효과는 명중과 같은 누적 규칙을 씁니다. 30% 효과는 두 번째 사용에서 처음 발동하고, 장기적으로 정확히 30% 비율이 됩니다. 확률 모드에서는 난수로 뽑습니다.
  - 풀죽음은 내가 먼저 움직였을 때만 걸립니다.
  - 우격다짐은 부가효과가 없고, 인분·방진망토는 막고, 하늘의은총은 확률이 2배입니다.
  - 결빙은 행동 전에 20%씩 녹고, 불꽃 기술이나 열탕에 맞으면 바로 녹습니다.
  - 2100 레이터 의견: "30% 풀죽음 기술은 여러 번 때리면 결국 발동한다. 그런 기술이 없으면 승리 확률의 큰 요건 하나를 버린 것이다." 이제 모델도 이 가치를 셉니다.
- **하품**: 다음 턴 끝에 잠듭니다. 실전에서는 상대가 교체로 피하므로 "한 턴 손해"로 보고 1턴만 잠재웁니다.
- **도발**(3턴 변화기 금지), **앙코르**(상대가 직전에 쓴 변화기에 3턴 고정)
- **벽**: 리플렉터·빛의장막·오로라베일(눈일 때만)은 5턴 반감이고, 빛의점토면 8턴입니다. 급소와 틈새포착은 벽을 무시합니다.
- **날씨·필드 기술**: 5턴이고, 전용 바위·그라운드코트를 들면 8턴입니다.
- **아직 값을 못 매기는 기술**(스텔스록·압정·길동무·트릭룸 등, 교체가 있어야 가치가 생김): 상대 대표 세트가 이런 기술을 들고 있으면, 그 칸을 다음 채용 공격기로 바꾼 변형과 **반씩 섞어** 평가합니다(C-2 단기안). 내 세트는 최적화가 이런 기술을 빼므로, 섞지 않으면 상대만 기술 3개로 싸우는 비대칭이 생깁니다.

**챔피언스 룰 반영** (09-26, GAME_RULES_REVIEW 대조)

| 룰 | 모델 |
|---|---|
| 마비 행동 불가 12.5%(본가 25%) | 확률 모드 12.5%, 기대값 모드 피해 ×0.875 |
| 잠듦 행동 불가 1턴(1/3)·2턴(2/3) | 확률 모드는 이 분포, 기대값·분기 모드는 최빈값 2턴 |
| 얼음: 매 턴 25% 해동, 최대 3턴 | 반영 |
| 시간 초과 = 무승부 | 60턴 안에 끝나지 않으면 0 |
| 날씨·필드 8턴 도구 | 기술과 특성(잔비·모래날림 등) 모두 날씨 바위·그라운드코트면 8턴. 빛의점토 벽 8턴 |
| 급소율 단계 | 급소율 높은 기술 +1, 대운 +1, 초점렌즈·예리한손톱 +1 → 1/24·1/8·1/2·확정. 스나이퍼 ×2.25 |
| 부가효과 확률 | 게임 설명문 우선(문포스 10%, 아이언헤드 20%, 셸암즈 10% — op.gg 기술 메타는 본가 수치) |
| 외형 변형 | 암컷 모습 등 능력치·타입·특성이 같은 폼은 기본 종으로 합침(메가진화·종 중복 판정). 돌핀맨은 제로폼으로 시작 |
| 메가 특성 | 부자유친(1타 + 0.25배 2타, 띠 관통), 하바네로분출(맞으면 공격자 화상), 압박감(PP 2 소모) |

**역할별 상대 세트** (09-26, 역할 분리 1단계)
- 레플리카 빌드 한 벌마다 역할을 판정합니다(`meta.classify_build`): 물리·특수·쌍두 공격형 / 물리막이 / 특수막이 / 양면막이 / 보조. 성격·스탯포인트와 기술 구성(공격기 수·범주)을 같이 봅니다.
- 한 개체(종 × 메가 여부)에서 빌드의 20% 이상·3벌 이상인 역할이 둘 이상이면 역할마다 대표 세트(메도이드)를 따로 만듭니다. 현재 26개체(조우 비중 22%): 드래펄트 특수 53%/물리 47%, 하마돈 물리막이 59%/양면막이 41%, 메가리자몽Y 특수 78%/물리 22% 등.
- 상대의 역할은 선출 때 내가 모르는 정보라 **베이지안 게임의 유형**으로 넣습니다. 내 계획·선출은 하나로 두고 상대만 역할별로 대응합니다. 역할별로 따로 최적 선출을 고른 뒤 평균하지 않습니다(그러면 역할을 미리 아는 셈).
- 그 종의 조우 가중치 합은 그대로라 여러 역할을 가진 종이 과대 계산되지 않습니다. 상대 유형 수는 340 → 427(계산 약 +25%).
- 아직: 내 카드의 역할별 보관(2단계), 배틀 중 공개 정보로 역할 추정 갱신·기믹 태그(3단계).

**특성 적용 범위** (op.gg 도감의 특성 설명과 대조해 점검했습니다)

| 분류 | 반영된 특성 |
|---|---|
| 타입 무효 | 부유·천정부지(땅), 흙먹기(땅), 타오르는불꽃·노릇노릇바디(불꽃), 저수·마중물·건조피부(물), 축전·피뢰침·전기엔진(전기), 초식(풀). 풍선도 땅 무효 |
| **기술 계열 무효** | **방탄**(구슬·폭탄 기술: 섀도볼·오물폭탄 등), **방음**(소리 기술: 하이퍼보이스·노래하기 등). 변화기에도 적용 |
| 데미지 경감 | 두꺼운지방, 내열, 수포, 정화의소금, 복슬복슬, 퍼코트, 아이스인분, 멀티스케일·섀도실드, 필터·하드록·프리즘아머, 건조피부(불꽃 ×1.25), 풀모피(풀 반감), 파동의방호(접촉 물리 반감) |
| 접촉 반격 | 까칠한피부·철가시(1/8), 울퉁불퉁멧, 정전기·불꽃몸·독가시(30% 상태이상), 미끈미끈(스피드 −1) |
| 기타 | 틀깨기류(방어 특성 무시), 배짱, 해감액(흡수기가 오히려 피해), 미러아머(능력 하락·위협 반사), 볼주머니, 젖은접시·촉촉바디·탈피, 황금몸·매직미러(변화기 무효), 여왕의위엄류(선공기 무효) |
| 09-26 추가 | 급류·맹화·심록·벌레의알림(HP 1/3 이하 ×1.5), 지구력(맞으면 방어 +1), 정의의마음, 심술꾸러기(랭크 변화 반전, 위협 포함), 트레이스(상대 특성 복사, 복사한 위협도 발동), 스나이퍼(급소 ×2.25), 대운(급소율 1/8), 복안(명중 ×1.3), 의욕(물리 명중 ×0.8), 이상한비늘, 속보, 서핑테일, 곡예(도구를 잃으면 스피드 ×2), 애널라이즈(늦게 행동하면 ×1.3) |

- 공격하는 쪽은 무효가 되는 기술을 아예 고르지 않습니다.
- 30% 확률 효과(정전기·탈피 등)는 확률 모드에서 30%로 뽑습니다. 기대값 모드에서는 누적 확률 1−0.7ⁿ이 0.5를 넘는 순간, 즉 두 번째 발동 기회에 적용합니다.
- **반영하지 않은 특성**: 교체나 더블 전용이라 1:1에 영향이 없는 것들입니다(재생력, 자연회복, 위기회피, 대접, 치유의마음, 아로마베일, 흡반, 미라·떠도는영혼, 나쁜손버릇, 헤롱헤롱바디).
- 흡수 계열 무효 특성(저수·피뢰침 등)의 회복·랭크업 부가효과는 넣지 않았습니다. 공격하는 쪽이 애초에 그 기술을 쓰지 않기 때문입니다.
- **내 세트의 특성은 고릅니다**(09-26). op.gg 최다 특성으로 최적화한 세트에 다른 합법 특성을 하나씩 넣어 채점하고, 0.02 안에 드는 특성은 그 특성으로 기술·배분을 다시 최적화합니다. 최다 특성보다 0.005 이상 나을 때만 바꿉니다(잡음 방지). 보고서의 세트 줄에 `특성:이름`으로 적고, 파티 파일에서 `특성:이름`으로 고정할 수 있습니다.
- 상대 세트의 특성은 실제 빌드(레플리카 한 벌)나 op.gg 최다 채용 특성을 씁니다.
- 아직 값이 없는 특성(재생력·자연회복처럼 교체 때 효과, 저주받은바디·변덕쟁이·포자처럼 확률 효과, 일루전, 자기과신·총대장처럼 여러 마리 싸움에서 효과)은 1:1에서 '특성 없음'과 같습니다. 이런 특성이 실제로 좋은 포켓몬은 과소평가될 수 있습니다.

**행동 규칙**
- "잡을 수 있다"는 실제로 들어갈 피해로 판정합니다. 기대값 모드는 명중을 곱한 피해, 확률 모드는 맞았을 때 피해입니다(예전에는 90% 기술의 맞았을 때 피해로 판정해서, 파멸의빛처럼 못 잡고 반동만 맞는 수를 골랐습니다).
- 잡을 수 있으면 잡습니다. 반동으로 같이 쓰러지지 않는 기술을 우선하고, 그다음 우선도·명중·반동이 적은 순입니다.
- 못 잡으면 (상대 남은 HP 중 깎는 비율 × 명중) − (내 HP 중 반동 비율) + 흡수 회복이 가장 큰 기술을 씁니다. 깎는 양은 상대 남은 HP에서 자르므로 넘치는 위력은 값이 없고, 반동으로 자멸하는 수는 피합니다.
- 회복은 **이번 행동 뒤 다음 한 대에 쓰러질 때만** 씁니다. 느린 쪽은 맞은 뒤 HP로 판단합니다. 그래서 더시마사리처럼 회복과 엉겨붙기를 교대로 씁니다.
- 먼저 쓰러진 쪽이 집니다. 턴 종료 화상·반동으로 둘 다 쓰러지면 먼저 쓰러진 쪽 기준입니다.

각 포켓몬은 계획을 하나 고릅니다. 계획은 다음 중 하나입니다.
- 그냥 때리기
- 쌓기 1~2회 후 때리기
- 상태이상 먼저
- 기습을 쓰지 않고 때리기: 기습 보유자만 해당합니다. 상대가 변화기로 기습을 헛치게 하는 수를 읽는 경우입니다.
- **버티기**: 회복기나 방어가 있을 때만 해당합니다. 회복량을 거의 다 쓸 만큼 깎이면 미리 회복하고, 상대가 턴마다 깎이는 상태(맹독·화상·속박·씨뿌리기)면 방어로 한 턴을 법니다. 공격은 그다음입니다. 예: 찌리배리가 게으름피우기로 버티다 충전 파라볼라차지로 역전하는 운영.
- 계획은 기술 순서로 자르지 않고 전부 봅니다. 예전에는 앞의 5개만 봤습니다.

계획 쌍마다 끝까지 돌려서 계획 행렬을 만들고, 그 **행렬 게임의 혼합전략 균형값**을 v ∈ [−1, 1]로 씁니다(이기면 +0.5 + 0.5×남은 HP). 안장점이 있으면 순수전략 값과 같고, 없으면(예: 도깨비불 ↔ 기습 읽기) `game.py`의 LP로 풉니다. 60턴 안에 안 끝나면 0.5×(내 HP 비율 − 상대 HP 비율)이고 범위는 ±0.5입니다. 3:3 맥락을 반영하려고 **정면 60% + 내가 교체로 들어가 한 대 맞고 시작 20% + 상대가 그렇게 들어옴 20%**로 섞습니다. 그래서 반대칭입니다.

**세 가지 모드**

| 모드 | 명중 | 난수·급소 | 쓰는 곳 |
|---|---|---|---|
| 결정적 | 피해에 곱하지 않고, 기술마다 빗나감을 누적해 0.5에 이르는 차례에만 빗나감(90% 기술은 5번째). 변화기도 같은 규칙 | 난수 0.925, 급소 없음(확정 급소기 제외) | 세트 최적화(평가 수십만 번이라 속도 우선) |
| **분기**(기본) | 결정적 규칙 위에, 한 판에서 **명중 판정 1번**을 맞음/빗나감 갈래로 나눔 | **문턱 타격 2번**(난수 0.85~1.00과 급소 1/24에 따라 KO 여부가 갈리는 타격)을 KO/비KO 갈래로 나눔 | 상성 행렬, 팀 탐색, pick, 선출 보드, check, audit |
| 확률 | 난수로 판정 | 난수·급소·마비·잠듦·연속기 타수(2~5회, 트리플악셀 타마다 명중)를 뽑아 N판 평균 | switch(64판), pick --mc |

- 분기 모드는 갈래를 확률로 가중 평균하고, 분기 예산을 다 쓰면 결정적 규칙으로 진행합니다. 결과가 결정적이라 캐시와 재현성이 유지됩니다.
- 확률 모드(32판) 대비 평균 오차가 결정적 0.093에서 분기 0.064로, 0.3 넘게 틀린 쌍이 12.4%에서 5.7%로 줄었습니다(상위 15종 105쌍). 계산량은 약 1.25배입니다.
- 명중 규칙의 배경: 피해에 명중률을 곱하면 90%짜리 확정 1타가 1타가 아니게 됩니다(예: 삼삼드래 용성군 183 vs 브리두라스 HP 167).
- **행동 결정은 명중·난수 결과를 미리 보지 않습니다**. "이번엔 빗나갈 차례"를 보고 기술을 바꾸지 않습니다.
- 빗나가거나 무효면 부가효과가 없습니다(용성군 특공 하락, 탁쳐서떨구기, 반동 턴 등). 무릎차기류는 빗나가면 최대 HP 1/2를 자해합니다.
- 급소는 방어 쪽 **상승**과 공격 쪽 **하락** 랭크만 무시합니다.
- 기습은 상대가 공격하지 않거나 **이미 움직였으면** 실패합니다.
- 킹실드·실크트랩·토치카·불꽃방패는 접촉한 상대에게 공격 −1, 스피드 −1, 독, 화상을 겁니다. 결정적이고 빠르며 세트 최적화와 팀 탐색에 씁니다. 확률 모드는 명중, 난수 0.85~1.00, 급소 1/24, 마비 25%, 잠듦 1~3턴을 N판 굴려 평균값과 **승률**을 냅니다. `switch`(기본 64판)와 `pick --mc N`에 씁니다.

엔진 점검: `python -m champions.trace 킬라플로르 더시마사리`로 한 판을 턴별로 재생합니다(계획 행렬과 턴마다의 HP 변화).

**실전 신호 혼합**: op.gg의 "많이 이긴 상대 / 많이 진 상대" 순위 차이를 신호 E로 만들어 `V = 0.8·시뮬 + 0.2·E`로 섞습니다. 두 목록 모두 상대 등장 빈도에 끌려가기 때문에 순위의 **차이**만 씁니다.

**검증** (자세한 원칙은 아래 "방법론"을 보세요)

`check`: 1:1 층만 봅니다. 상위 50종에서 op.gg 실전 신호 E가 있는 1,014쌍의 스피어만 ρ이고, 구간은 **종 단위 군집 부트스트랩** 95%입니다.

| 예측자 | ρ(·, E) | 95% 구간 |
|---|---|---|
| 1:1 시뮬레이터 | +0.636 | [+0.54, +0.73] |
| 기준선: 타입 상성 한 줄 계산 | +0.515 | [+0.39, +0.62] |
| **증분**(시뮬 − 기준선) | **+0.12** | **[+0.02, +0.24]** |
| 타입을 통제한 부분상관 | +0.50 | — |

- 시뮬레이터는 타입 상성보다 낫지만 **증분은 겨우 유의**합니다.
- 버전 사이의 ρ 변화(0.632 → 0.645 → 0.628 → 0.639 …)는 **모두 구간 안의 잡음**입니다. 예전에 이를 "개선"으로 적었던 것은 쌍 독립 가정으로 불확실성을 과소평가한 탓이라 철회합니다.
- 이 지표로 엔진을 여러 번 조정해 왔으므로 **더 이상 표본 외 지표가 아닙니다**. 그래서 종을 고정 시드(20260926)로 A(조정용)/B(보류용) 묶음으로 나눴습니다. 앞으로 조정 판단은 A 묶음으로만 하고, B 묶음은 보고에만 씁니다.

`audit`: 외부 심판(gpt-6-astra)과 1:1 승패 방향 비교입니다.
- seed 8에서 19/20, Wilson 95% [0.76, 0.99]입니다. seed 8은 그동안 수정의 기준이 된 표본이라 낙관적인 값이고, 공정한 비교는 새 시드(유료)입니다.
- 20쌍 사이의 차이(예: 17/20 → 20/20, 피셔 정확검정 p = 0.23)는 통계적으로 구분되지 않습니다.
- 심판은 기준값이 아닙니다. 강철 → 페어리 2배를 빠뜨린 사례도 있고, 모델과 같은 단순화를 써서 공통 편향은 잡지 못합니다.

`python -m champions.selftest`: 엔진 규칙 **회귀 테스트** 12개입니다(명중·부가효과 누적, 빗나감 후처리, 방탄, 멀티스케일 첫 타, 급소 랭크, 킹실드, 미결 판정, 반대칭, 게임 풀이기). 엔진을 고친 뒤 항상 돌립니다.

**상대 메타**: 상대 한 개체는 "포켓몬 × 메가 여부" 단위입니다.
- 대표 세트: 레플리카 팀에 같은 개체 빌드가 3개 이상 있으면, **그중 나머지와 가장 닮은 실제 빌드 한 벌(메도이드)을 통째로** 씁니다.
  - 예전에는 성격·배분·도구·기술을 항목별 최빈값으로 따로 뽑아서, 아무도 쓰지 않는 조합이 생길 수 있었습니다.
  - 실제 세트라서 스텔스록·하품·방어처럼 시뮬이 값을 못 매기는 기술도 그대로 들어갑니다.
- 레플리카 빌드가 부족하면 op.gg 최다 배분에, 그 배분과 맞는 성격 중 최다를 짝지웁니다.
- 조우 가중치: 레플리카 싱글 팀(**중복 제거**)에 나온 횟수에 싱글 순위 사전값을 더해 정합니다.
- **범위는 op.gg 싱글 순위 전 종**입니다(M-6: 262종, 레플리카에만 있는 변형 폼 포함 시 상대 269종·메가형 포함 개체 345개).
  - 순위가 낮은 포켓몬도 빠지지 않습니다. 사전값에 바닥값을 두어 작은 가중치로 들어갑니다.
  - 팀 후보 풀은 **싱글 200위 이내**입니다(`--max-rank N`, 0이면 제한 없음). 200위 밖은 실전에서 거의 쓰이지 않아 추천하지 않습니다. 상대로는 계속 계산하고, `--must`로 직접 넣은 포켓몬은 순위와 관계없이 들어갑니다.
- 도감에서 빠지는 것은 외형 변형 8종뿐입니다(암컷 모습, 깃털색 등). 이들은 op.gg 순위나 통계가 따로 없습니다.
- **메타몽(괴짜)**: 등장하자마자 상대로 변신합니다. HP를 뺀 능력치·타입·특성·기술(PP 5)을 복사하고 자기 도구(스카프·띠 등)는 유지합니다. 세트 최적화는 도구와 HP 투자만 고릅니다(`변신형`). 계획(쌓기·버티기 등)은 **변신한 뒤의 기술**로 세웁니다(09-26 수정 전에는 '때리기' 하나뿐이라 상대의 칼춤·용의춤을 따라 쓰지 못했습니다). 1:1은 두 마리가 새로 만나는 상황이라, 메타몽의 핵심 가치인 **이미 능력을 올린 상대를 복사**하는 장면은 담지 못합니다(3:3 연속 평가 C-1에서 다룰 부분). 이 몫은 op.gg 실전 신호(λ)로만 일부 들어갑니다.

**세트 최적화** (`sets.py`): 실채용 최다 세트에서 출발해 한 칸 바꾸기를 개선이 없을 때까지 반복합니다(정면 1:1 값, 빠름). 그다음 도구, 성격·스탯포인트를 교체 등장까지 섞은 값으로 고릅니다. 마지막으로 **같은 목표(교체 등장 포함)로 기술을 한 번 더 다듬어** 기술과 도구의 목표를 맞춥니다. 후보는 다음과 같습니다.

- op.gg 채용 3% 이상인 기술
- 배울 수 있는 공격기 중 타입·분류별 최강 1개
- 쌓기·회복·상태이상기는 채용 10% 이상일 때만

채용 70% 이상 기술은 고정합니다. 모델이 값을 매기지 못하는 기술(스텔스록·방어·교체기의 교체 이득 등)이 여기에 해당하기 때문입니다.

**공격 유형**: 세트는 유형을 먼저 정하고, 성격·스탯포인트·기술이 모두 그 유형과 맞아야 후보가 됩니다. 예전에는 특수 투자(조심, C32)를 한 망나뇽에게 무투자 지진·신속이 들어가는 식으로 의도가 불분명한 세트가 나왔습니다.

| 유형 | 성격 | 스탯포인트 | 공격기 | 쌓기기 |
|---|---|---|---|---|
| 물리형 | 공격↓·특공↑ 금지 | 특공 ≤ 4 | 물리만 | 공격을 올리거나 방어·스피드만 올리는 것 |
| 특수형 | 특공↓·공격↑ 금지 | 공격 ≤ 4 | 특수만 | 특공을 올리거나 방어·스피드만 올리는 것 |
| 쌍두형 | 공격↓·특공↓ 금지 | 공격·특공 각 ≥ 8 | 물리 1개 이상 **그리고** 특수 1개 이상 | 제한 없음 |

- 종족값이나 op.gg 채용상 말이 되는 유형마다 따로 최적화하고, 가장 높은 유형을 추천합니다. 높은 쪽 공격 스탯의 유형은 항상 보고, 낮은 쪽은 그 스탯이 높은 쪽의 80% 이상이거나 그 분류 공격기 채용 합이 25% 이상일 때만 봅니다. 쌍두형은 두 유형이 모두 가능할 때 봅니다.
- `moves` 출력에 유형별 최고 점수를 같이 보여줍니다. 파티 파일에 `물리형`/`특수형`/`쌍두형`을 적으면 그 유형으로만 추천합니다.
- 고정 기술(채용 70% 이상이거나 `--keep`)이 유형과 충돌하면 그 유형은 제외합니다. 모든 유형과 충돌하면 쌍두형으로 처리합니다.
- op.gg에서 가져온 상대 대표 세트는 실제 채용을 합친 것이라 유형 규칙을 적용하지 않습니다. 표시는 `비정형(…)`으로 나올 수 있습니다.

**역할**: 공격 분류와 별개로, 세트가 "때리는 쪽"인지 "버티는 쪽"인지 정합니다. 표시는 `물리 공격형`, `특수 내구형`처럼 합쳐서 나옵니다.

| 역할 | 스탯포인트 | 성격 | 도구 |
|---|---|---|---|
| 공격형 | 공격 스탯 ≥ 16 (쌍두형은 둘 다 ≥ 8) | 공격 스탯이나 스피드를 올리거나 무보정 | 생명의구슬·구애스카프·기합의띠·달인의띠·타입 강화 도구는 공격형만 |
| 내구형 | 공격 스탯 < 16, HP + (방어 또는 특방) ≥ 40 | 방어나 특방을 올리고, 둘 다 내리지 않음 | 울퉁불퉁멧은 **물리 방어 투자(방어 ≥ 16 또는 방어↑ 성격)가 있는 내구형만** |

- 먹다남은음식·자뭉열매·리샘열매 등은 두 역할 모두 쓸 수 있습니다.
- 구애스카프는 쌓기·회복·변화기와 같이 쓰지 않습니다. 기술이 하나로 고정되기 때문입니다.
- 내구형은 회복기가 후보에 있거나 실제 내구형 배분이 관측될 때만 따로 최적화합니다. 쌍두형은 공격형만 봅니다.
- 성격·스탯포인트 후보 순서:
  1. **레플리카 팀에 실제로 같이 쓰인 (성격, 배분) 짝**
  2. op.gg 성격 × op.gg 배분 조합 중 규칙을 통과하는 것
  3. 역할별 정석(고집 A32 S32, 장난꾸러기 H32 B32 등)
- 예전에는 op.gg가 성격과 배분을 따로만 줘서 둘을 곱해 조합을 만들었습니다. 그 결과 "고집 + H31 B25 S10(원래는 장난꾸러기 헬멧 망나뇽의 배분) + 울퉁불퉁멧 + 공격기 4개"처럼 아무도 쓰지 않는 세트가 나왔습니다.

**팀 탐색** (`team.py`): 선출 게임은 서로 6마리를 보고 3마리씩 내는 방식입니다. 내 3 대 상대 3의 값은 "상대 한 마리마다 내 최선의 답"과 "내 한 마리마다 상대의 최악 카운터"의 평균입니다. 팀 대 팀 값은 20×20 선출 게임의 값이고, 팀 점수는 이 값을 레플리카 싱글 팀 전체에 대해 평균한 것입니다. 제약은 종 중복 금지, 아이템 중복 금지, 메가 스톤 1장 이하입니다.
- 탐색: 탐욕 시작과 무작위 재시작 후 한 장 바꾸기로 국소 탐색합니다. 이어서 **같은 6종의 도구 변형 조합을 전부**(최대 3^6) 봅니다. 한 장 바꾸기로는 못 하는 "두 마리 도구 맞바꾸기"를 처리하기 위해서입니다. 이 단계의 게임 값은 속도 때문에 순수전략 하한·상한 평균(근사)을 씁니다.
- 최종: 상위 20개 후보를 **혼합전략 균형값(LP, 정확)**으로 다시 줄 세웁니다. 상대 팀 표본을 부트스트랩해 후보별 1위 확률과 표준오차도 구합니다. 탐색에 쓴 표본을 다시 써서 1위 확률은 낙관적입니다(승자의 저주).
- `pick`도 같은 20×20 게임을 LP로 풉니다. 균형에서 가장 자주 낼 3마리를 추천하고, 선출 혼합 비율과 상대 선출 예상 분포를 같이 보여줍니다.

추천 팀은 이렇게 고릅니다.
- 먼저 1위와 통계적으로 구분되지 않는 후보를 모읍니다. 기준은 **같은 상대 표본에서의 대응 차이**가 2 표준오차 안입니다. 후보들은 멤버를 대부분 공유하므로 팀별 SE가 아니라 이 대응 차이로 판정합니다.
- 그 안에서 무답 노출(값 ≥ 0인 카드가 2장 미만인 상대의 비중)이 1%p 이상 작은 팀을 우선하고, 나머지는 팀 값이 높은 팀을 고릅니다.

## 방법론 — 무엇을 추정하고, 얼마나 믿을 수 있나

2026-09-25 방법론 검토(데이터 분석·수리 모델 관점, 판정 "대폭 수정")를 반영해 적습니다.

**추정 대상**
- 이 모델이 원래 추정하려는 양은 "상대 팀 분포 D 아래에서, 정해진 선출 정책을 따를 때 내 팀의 승률 P(승 | 내 팀, D)"입니다.
- 지금의 팀 값·선출 값은 이 승률이 **아닙니다**. 다음 선택을 차례로 합성한 비교용 점수입니다.
  - 1:1 효용 척도(이기면 +0.5 + 0.5×남은 HP)
  - 정면 60 / 교체 등장 20 / 20 혼합
  - 실전 신호 λ = 0.2 결합
  - 선출 합산식
  - 20×20 게임 값
  - 레플리카 팀 평균
- 그래서 **후보 사이의 순위와 차이만** 의미가 있고, 절대값(예: +0.23)은 해석하지 않습니다. 보고서도 소수 둘째 자리로 줄였습니다.
- 승률로 보정하려면 실전 결과(상대 6, 내 선출·선봉, 승패)가 필요합니다. 선출 보드에서 기록을 모으는 것이 다음 단계 후보입니다.

**층별 검증 상태**

| 층 | 검증 |
|---|---|
| 1:1 매치업 | `check`(실전 신호, 군집 구간·기준선·보류 묶음), `audit`(심판, 20쌍), 회귀 테스트 |
| 교체 등장 가중·선출 합산식·선출 게임 | **외부 검증 없음**. 3:3 연속 평가 시제품과 비교하면 선출 추천이 10경기 중 3경기만 일치합니다(C-1, 나중에) |
| 팀 추천 | **외부 검증 없음**. 모델 자신의 목적함수로만 평가하므로 우월성 주장은 순환 논증입니다 |

**불확실성 읽는 법**
- 팀 보고서의 "선택 빈도"와 SE는 **상대 팀 표본**의 불확실성만 담습니다. 모델이 맞다는 조건부 값이라 "이 팀이 최적일 확률"이 아닙니다.
- 실제로는 **가정 쪽 불확실성이 지배**합니다. `python -m champions sensitivity`로 λ(0/0.2/0.4), 교체 가중(0/0.1/0.2/0.3), 효용 척도(승패 부호만)를 바꿔 보면, 멤버별로 몇 개의 변형에서 살아남는지 알 수 있습니다.
- 팀 보고서의 "코어 + 유동 자리"도 같은 취지입니다.

**게임 정식화**
- 1:1은 **열린 루프 계획 게임**입니다. 처음에 계획(그냥 때리기·쌓기·버티기 등)을 정해 끝까지 따르므로, 매 턴 반응하는 실제 전투(닫힌 루프)의 근사이고 오차의 방향도 모릅니다.
- 상대는 **최소최대 최적 선출**을 한다고 가정합니다(강건 기준). 실제 상대는 휴리스틱으로 고릅니다.
- 추천 팀은 현재 스냅숏 메타에 대한 **최선 응답**입니다. 메타가 내 선택에 반응하는 균형이 아닙니다.
- **숨은 정보는 베이지안 게임**으로 풉니다. 상대의 메가가 누구인지, 상대가 대표 세트와 변형 세트 중 무엇을 쓰는지를 상대는 알고 나는 모릅니다. 확률로 먼저 평균하면 게임 값이 낙관 쪽으로 치우치기 때문입니다(Jensen). `game.solve_bayes`가 이중 오라클로 풉니다.

**데이터의 한계**
- 레플리카 팀은 자기선택 표본이라 래더 모집단을 대표하지 않습니다.
- op.gg 통계는 시즌 누적이라 최신 메타에 가중되지 않습니다.
- 기술 통계는 상위 10개, 승패 목록은 상위 30개로 잘려 있고, 결측을 단일 값으로 채웁니다.
- 층마다 쓰는 상대 모집단이 다릅니다.
  - 세트 최적화: 조우 가중 상위 80
  - 팀 점수: 레플리카 팀 균등 평균
  - 무답 노출: 조우 가중 전체
  - check: 상위 50종 쌍

**아직 반영하지 않은 권고**
- 상대 빌드 혼합: 대표 세트 1벌 대신 빌드 군집 2~3개를 섞는 방식
- 닫힌 루프 근사 오차 측정
- 실전 신호 E의 척도 정합과 계층 베이즈 결합
- 실전 승패 보정
- 3:3 연속 평가(C-1)

**재현**
- 보고서 끝에 모델 코드 지문, 원본 데이터 지문, numpy 버전을 적습니다.
- `requirements.txt`에 의존성을 고정했습니다.
- LLM audit은 비결정적이고 유료라 제3자 재현이 어렵습니다.

## v5에서 바뀐 점

| v5 | v6 |
|---|---|
| Node 전용. 이 PC에 Node가 없어서 실행 불가 | Python + numpy |
| 스크래퍼 없음(09-17 수집본 고정) | `scrape` 한 번으로 최신 시즌 수집 |
| 턴수 공식 `o = ((n_j−n_i)+선공)/3`. 선공 ±1턴 오류로 21% 칸의 승패 부호가 틀림(`legacy_v5/VALIDATION_v5_1.md` §1-1) | 턴 단위 시뮬레이션이라 선공은 순서로만 들어감 |
| 대리점수로 600팀을 자른 뒤 LP. 대리점수와 LP 값의 상관은 ≈0 (§1-2) | 팀 점수를 선출 게임으로 직접 탐색(numpy 벡터화) |
| 상대 메가 확률을 별도 보정 | 개체 단위(메가 여부)로 모델링하고 레플리카 팀에서 누가 메가인지 직접 읽음 |
| 실전 승패 데이터 미사용 | op.gg 승패 상대 순위를 20% 혼합하고 점검 지표로도 씀 |
| 6스탯 동시 252 가정 | 실제 스탯포인트(합 66) 분포 사용 |

## 한계

- **모델 밖**: 3:3을 이어서 도는 평가(C-1, 나중에). 교체, 설치기(스텔스록 등)와 그 제거, 기합의띠 소모가 다음 매치업으로 넘어가는 것, 교체 이득(유턴·볼트체인지·재생력), 트릭룸, 혼란이 해당합니다. 팀 값은 여전히 1:1 값의 선출 게임 합산입니다.
- 2100 레이터 의견 중 "선출은 크게 이기는 조합보다 안정적으로 받아내는 조합"과 "구애스카프 선출은 도박에 가깝다"는 HP가 이어지는 3:3 평가(C-1)가 있어야 제대로 반영할 수 있습니다. 1:1 끝장 모델은 스카프의 기술 고정 손해(읽힘, 교체 강요)를 과소평가합니다.
- op.gg 승패 신호의 혼합 가중치 0.2는 근거가 약합니다(ASTRA 지적 3). 신호 자체가 검증 지표라서 같은 데이터로 튜닝하면 과적합이 됩니다.
  - 09-23과 09-24 수집본은 독립 데이터가 아닙니다. 두 날짜 신호의 상관이 0.997이고 71%의 쌍이 값까지 같습니다(op.gg 통계는 시즌 누적).
  - 최소 몇 주 떨어진 수집본이나 다음 시즌 데이터로 0~0.4를 비교해 정할 예정입니다.
- `switch`는 랭크 변화·필드·남은 파티 구성을 반영하지 않습니다(ASTRA 지적 4, 3:3 시뮬레이션과 함께 보류).
- 명중·급소·난수 분산은 `switch`와 `pick --mc`에서만 확률로 봅니다. 팀 탐색과 세트 최적화는 기대값 모드입니다.
- 모든 "최적/추천"은 이 가정 아래의 **후보**입니다. 승률 예측이 아닙니다.
- 레플리카 팀은 사용자가 제출한 팀이라 래더 전체와 분포가 다를 수 있습니다.
- op.gg 기술 통계는 상위 10개만 줍니다. 그 밖의 기술은 배울 수 있는 기술 목록에서 후보로 넣습니다.
- 메가 가능 여부나 스톤 판정은 op.gg 도감 기준입니다(시즌 M-6: 메가 82종).

## 폴더

```
champions/        코드  (scrape · data · engine · meta · sets · matrix · team · pick · textio · pipeline · audit · trace · __main__)
data/raw/<날짜>/  수집 원본
data/processed/   세트·행렬 캐시 (수집일/<모델 코드 지문>-<원본 데이터 지문> — 코드를 고치거나 같은 날 다시 수집하면 자동으로 다시 계산)
out/              결과 리포트
legacy_v5/        이전 버전 전체(코드·CSV·행렬·검증 보고서·페이지)
```
